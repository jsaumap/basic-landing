import { Grid } from '@mui/material';
import type { NextPage } from 'next';
import { Layout } from '../components/layouts';

const HomePage: NextPage = () => {
  return (
    <Layout title='Home - BookingMe'>
      <Grid container spacing={2}>

      </Grid>
    </Layout>
  );
};

export default HomePage;
